{
  "name": "stream-proxy-function",
  "version": "1.0.0",
  "description": "A proxy function for HLS streams",
  "main": "stream-proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
